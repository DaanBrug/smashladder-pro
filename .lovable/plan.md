Seed `daanbrug70@gmail.com` into the `public.app_admins` allowlist table so the existing `is_admin()` check grants `/admin` access on first login. No code changes required; future admins can be added by inserting more rows (or via an optional in-app admin-management UI later).